import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "معتمد | پلتفرم انعقاد قرارداد",
  description: "پنل مشتریان معتمد",
};

const RootLayout = ({ children }: React.PropsWithChildren) => <>{children}</>;

export default RootLayout;
